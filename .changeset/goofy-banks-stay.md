---
"@playwright-labs/otel-core": patch
---

Add README documenting all public exports — `Counter`, `Histogram`, `UpDownCounter`, `Span`, `OtelEvent`, `resolveOtelConfig`, and `createOtelSdk` — with the stdout bridge architecture, `using` keyword lifecycle, and SDK factory examples.
